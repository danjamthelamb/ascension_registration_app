<?xml version="1.0" encoding="utf-8"?>
<network-security-config>

    <!-- DEV only: allow HTTP traffic to the local LAN gateway. -->
    <base-config cleartextTrafficPermitted="true" />

</network-security-config>